# 야수 사냥꾼 (BM) — KeySim 로테이션 규칙

**작성일:** 2026-08-14
**상위 문서:** [`midnight-rotation-handoff.md`](midnight-rotation-handoff.md) — 아키텍처·색 슬롯 프로토콜·CD 운용 원칙은 그쪽을 따른다.
**근거:** `simulationcraft/simc` `midnight` 브랜치 **라이브 재확인 (2026-08-14)** + Icy Veins / Wowhead 12.1 교차 확인
로컬 스냅샷(`~/PersonalProjects/simc-apl-research/`, 커밋 `f50a2121bf89`, fetch 2026-08-14)은 `default`·`assisted_combat` 모두 라이브와 일치한다 — 2026-08-18 재확인. 낡았던 커밋 `543891d`(fetch 2026-08-03)는 `.backup-20260803/`로 밀려났고 재fetch는 이미 완료됐다.
**전제:** AC(JustAC) 추천을 기본 필러로 두고 그 위에 예외 레이어를 얹는 핸드오프 §3.3 구조.

---

## 1. 기본 진단

Assisted Combat APL은 조건이 붙은 줄이 **Wild Thrash 두 줄뿐**이고 나머지는 "쿨 돌면 누른다"이다.
default APL과의 갭은 거의 전부 **야수의 격노(30초) 창에 맞춘 뱅킹 로직**으로 수렴한다.
핸드오프 §5.4의 "BM: default에 BW 패킹, assisted는 평탄 → S 아님" 판정과 일치한다.

---

## 2. 확정 규칙

```
1. 격노 쿨 1.5초 이내 OR 날사 2중첩            → 날사
2. 난타 / 회전베기                             → AC 추천 추종 (적 수는 AC)
3. 포효 준비                                   → 살상
4. 동맹 up AND 격노 쿨 1.5초 초과
   AND (포효 쿨 4초 초과 OR 살상 2중첩 근접)   → 살상
5. 격노 쿨 1.5초 이내 AND 위 전부 불가         → 대기
6. 이하 AC 추천 추종
```

| 규칙 | 근거 (라이브 `actions.st`) | 비고 |
| --- | --- | --- |
| 1 | 1순위 `barbed_shot, if=cooldown.bestial_wrath.remains<gcd\|full_recharge_time<gcd` — `bestial_wrath`(2순위)보다 위 | 한 줄에 **조건 둘**이 OR로 들어 있다. `remains<gcd`는 **패킹**(충전 개수 무관 — 1충전이어도 사용), `full_recharge_time<gcd`는 **오버캡 방지**. 둘을 하나로 뭉뚱그리지 말 것 |
| 2 | AC가 난타·회전베기를 고름 | 키심에 적 수 조건을 넣지 않는다. 2~3타 공백은 AC 천장으로 남김 |
| 3 | 4순위 `kill_command, if=howl_summon.ready` — 조건 이것뿐 | 포효가 준비되면 동맹도 격노 게이트도 필요 없다 |
| 4 | 5순위 `kill_command, if=(cooldown.bestial_wrath.remains>full_recharge_time+gcd&buff.natures_ally.react\|!apex.3)&(buff.howl_of_the_pack_leader_cooldown.remains>4\|cooldown.kill_command.charges_fractional>1.8)` | 게이트가 **둘**이다. 앞 괄호 = 격노 뱅킹 + 동맹, 뒤 괄호 = **포효 뱅킹**. 2중첩(`charges_fractional>1.8`)은 **뒤 괄호만 푼다** — 동맹 요구는 남는다 |
| 5 | 8순위 `cobra_shot, if=cooldown.bestial_wrath.remains>gcd` 가 실패하면 SimC도 idle | 격노 직전이라도 난타·포효 살상·송곳니 코브라는 **차단되지 않는다**(해당 줄에 격노 게이트가 없음). 대기는 최하위 폴백이다 |

**`!apex.3` 주의.** 규칙 4의 앞 괄호는 `... & 동맹 | !apex.3` 이므로, **정점 3을 찍지 않은 빌드에서는 격노 게이트와 동맹 요구가 통째로 풀린다**(살상 무조건). 정점 3을 찍은 일반 빌드에서는 동맹이 필요하다. 프로필 확정 전 본인 빌드에서 확인할 것.

---

## 3. 핵심 비대칭 — 날사와 살상은 방향이 반대

| | 격노 직전 | 근거 |
| --- | --- | --- |
| 날사 | **비우고 들어간다** | `barbed_shot, if=cooldown.bestial_wrath.remains<gcd` — 격노보다 위, 충전 조건 없음 |
| 살상 | **아끼고 들어간다** | `kill_command, if=cooldown.bestial_wrath.remains>full_recharge_time+gcd&...` — 2중첩 근접이어도 격노가 `full_recharge_time+GCD` 이내면 누르지 않음 |

**게이트 폭 주의.** `charges_fractional>1.8`은 `full_recharge_time`을 0으로 만들지 **않는다.** 0이 되는 것은 `charges_fractional == 2.0`(완전 2중첩)일 때뿐이고, `>1.8` 구간의 `full_recharge_time`은 `(0, 0.2×재충전시간]`이다. 따라서 살상 억제 구간은 "격노 1.5초 이내"가 아니라 **`격노.remains ≤ full_recharge_time + GCD`** 이며, 2중첩 근접에서도 이 폭이 남는다. 키심은 2충을 `격노준비` 행과 `임박·준비 둘 다 꺼짐` 행으로 나누고, 1충은 준비(`10`)만 쓴다. `살상안전`(`격노.remains > 살상.full_recharge_time + GCD`)은 12.1에서 두 쿨을 비교해 못 그려, 중쿨 1충은 버린다.

APL이 왜 이렇게 배치됐는지(메커니즘)는 확인되지 않았다. 격노는 `Modifies Damage/Healing Done 20%`로 사냥꾼 본체 스킬에도 적용되므로 "날사는 격노 버프를 안 받는다"는 설명은 성립하지 않는다. **추정으로 채우지 말고 위 두 줄의 사실만 근거로 쓸 것.**

---

## 4. Wild Thrash 사실 메모

| 항목 | 값 |
| --- | --- |
| Wild Thrash 재사용 대기시간 | 8초 |
| 야수 회전베기 지속시간 | 10초 (12.1에서 8초 → 10초 상향) |
| 겹침 | 2초 |

- AC의 `buff.beast_cleave.down` / `remains<=2` 두 줄은 겹침이 정확히 2초이므로 **"쿨 돌면 즉시"와 동치**다. AC의 타이밍 판정 자체는 정확하고, 결함은 타겟 수 임계뿐이다.
- 따라서 **회전베기 잔여시간 색 슬롯은 불필요**하다. Wild Thrash 쿨만 보면 된다.
- Wild Thrash는 유지기가 아니라 딜기(공격력 191%, 2대상 이상 시 ×3)다. 버프 만료까지 늦추면 주기가 8초 → 10초가 되어 **캐스트 수가 20% 줄고, 얻는 것은 없다**(유지율은 양쪽 다 100%). 구 일제 사격의 감각을 적용하지 말 것.

---

## 5. 예외

**어둠 순찰자 광역(`actions.drcleave`).** 날사가 격노(2순위) 아래 5순위이므로 이 구간에서는 규칙 1을 끈다.

진입 조건은 `talent.black_arrow&(active_enemies>2|talent.beast_cleave&active_enemies>1)` 이다 — **회전베기 특성이 있으면 2타겟부터** drcleave로 들어간다(3타겟 아님). 단일 대상과 무리의 지도자는 해당 없음.

---

## 6. 남은 추가 항목

| 순위 | 규칙 | 대응 default 줄 |
| --- | --- | --- |
| 1 | 부패 잔여 2GCD 미만 → 통곡 | 단일 `wailing_arrow, if=buff.withering_fire.remains<execute_time+2*gcd`<br>광역은 `+gcd`(1GCD) |
| 2 | 부패 up AND 살상 충전 여유 → 검화 | `black_arrow, if=buff.withering_fire.up&cooldown.kill_command.full_recharge_time>gcd` |
| 3 | 송곳니 3중첩 이상 → 코브라 | 단일 `cobra_shot, if=buff.cobra_fang.stack>=3`<br>어둠 순찰자 `if=buff.cobra_fang.stack>3`<br>광역 `if=buff.cobra_fang.up&buff.beast_cleave.remains` |

> 항목 3은 **Hogstrider(멧돼지 기수) 조건을 대체한 것**이다. 라이브 `actions.cleave`에서 `hogstrider` 줄은 삭제되고 송곳니로 교체됐다.

---

## 7. 구현 불가 / 자동화 제외

| 항목 | 사유 |
| --- | --- |
| `target_if=min:dot.barbed_shot.remains` (날사 타겟 스와핑) | 타겟 전환 필요 — KeySim 범위 밖 |
| `charges_fractional<1.4` (살인 코브라 분기) | **구현 불가가 아니라 범위 밖** — `actions.drst`(어둠 순찰자) 전용 줄이다. Pack Leader `actions.st`에는 없다 |
| `focus<75` (§9 `11 날사_잔여`의 원문 조건) | 연속값 — 이산 색 플래그로 옮기기 어려움. `talent.serpentine_strikes` 채용 시에만 무조건이 되어 이 조건이 사라진다 |
| 격노 2GCD 전 난타 선행 정렬 | 미래 예측 필요, default APL도 미모델링 → 수동 판단 |
| 장신구 · 물약 · 종족특성 | 핸드오프 §4 원칙 유지 (전부 `cooldown.bestial_wrath.ready` 정렬) |
| 차단 · 생존기 · 소환수 관리 · 사냥꾼의 징표 | 상황 판단 |

---

## 8. 격노 자동 — 애드온 토글 박스

KeySim runtime toggle은 쓰지 않는다. 애드온이 토글 상태를 박스로 그리고, 키심은 `[조건] 격노토글`로만 본다. 박스 꺼짐 = 홀드(`05` 불성립), 켜짐 = 자동 격노. 임박은 ready를 포함하지 않아 홀드 중 필러가 멈추지 않는다. 키 `5` 수동은 조건과 무관하다.

---

## 9. 키심 이벤트·조건 (DPS 핵심만)

전제: **Pack Leader + 정점 3.** SimC 전부가 아니라 격노 창·충전 비대칭·포효/동맹 살상·광역 난타만. 그룹 `야수`. 조건은 AND만. 센서는 `execute_action=False`.

입력 **우선순위는 전부 `1`.** 그룹 선택은 `min((priority, name))`이므로 순서는 **이름 접두 `01`–`08`, `10`–`14` iteration**이다 (`09` 없음). 접두를 바꾸면 순서가 바뀐다.

화면에는 **임박 1GCD 박스**와 **격노 사용 가능 박스**만 있다. `[조건] 격노여유1.5` 단일 센서는 쓰지 않는다.

- `[조건] 격노임박1.5` := `0 < remains ≤ 1GCD` (준비와 금지)
- `[조건] 격노준비` := ready
- “여유” = 준비이거나 (임박 아님 ∧ 준비 아님). AND만 되므로 입력 행을 **준비 / 대기**로 나눈다.

홀드(ready·`[조건] 격노토글` 꺼짐) 때는 `05`만 멈추고 `*준비` 행이 살상·코브라를 유지한다.

프로필 `runtime_toggle_enabled`는 끈다. `05`는 멤버가 아니다.

### 9.1 감지

| 이벤트 | 점등 | 겹침 |
| --- | --- | --- |
| `[조건] 날사2` | 날사 `full_recharge_time < GCD` | 다음 충전이 아님 |
| `[조건] 날사1` | 날사 ≥1 | `[조건] 날사2`와 겹침 |
| `[조건] 살상2` | 살상 `charges_fractional > 1.8` | |
| `[조건] 살상1` | 살상 ≥1 | `[조건] 살상2`와 겹침 |
| `[조건] 격노임박1.5` | `0 < 격노.remains ≤ 1GCD` (1.5초 근사) | **준비와 금지** |
| `[조건] 격노준비` | 격노 ready | 임박과 금지 |
| `[조건] 격노토글` | 애드온 자동 격노 허용 박스 | 꺼짐 = 홀드 |
| `[조건] 동맹` | 자연의 동맹 | |
| `[조건] 포효` | `howl_summon.ready` | |
| `[조건] 포효임박4` | 포효 쿨버프 `0 < remains ≤ 4`, 아직 준비 아님 | **`[조건] 포효`와 금지** |
| `[조건] 송곳니3` | 송곳니 ≥3 | |
| `[조건] 난타레디` | 난타 쿨 준비 | |
| `[조건] 다수2` | 적 ≥2 | |

### 9.2 입력 (전부 `priority=1`)

| 이벤트 | 키 | 활성 | 비활성 |
| --- | --- | --- | --- |
| `01 날사_캡` | 날사 | `[조건] 날사2` | |
| `02 날사_패킹임박` | 날사 | `[조건] 날사1` `[조건] 격노임박1.5` | `[조건] 다수2` |
| `03 날사_패킹준비` | 날사 | `[조건] 날사1` `[조건] 격노준비` | `[조건] 다수2` |
| `04 난타` | 난타 | `[조건] 난타레디` `[조건] 다수2` | |
| `05 격노` | 격노 | `[조건] 격노준비` `[조건] 격노토글` | |
| `06 살상_포효` | 살상 | `[조건] 포효` `[조건] 살상1` | |
| `07 살상_동맹2준비` | 살상 | `[조건] 동맹` `[조건] 살상2` `[조건] 격노준비` | |
| `08 살상_동맹2대기` | 살상 | `[조건] 동맹` `[조건] 살상2` | `[조건] 격노임박1.5` `[조건] 격노준비` |
| `10 살상_동맹1준비` | 살상 | `[조건] 동맹` `[조건] 살상1` `[조건] 격노준비` | `[조건] 살상2` `[조건] 포효임박4` |
| `11 코브라_송곳니` | 코브라 | `[조건] 송곳니3` | |
| `12 날사_잔여` | 날사 | `[조건] 날사1` | |
| `13 코브라준비` | 코브라 | `[조건] 격노준비` | |
| `14 코브라대기` | 코브라 | | `[조건] 격노임박1.5` `[조건] 격노준비` |

**키 자리표시.** 실제 인게임 바인드가 정해지기 전까지 쓰는 임시값이며, 순서는 **실측 캐스트 빈도** 기준이다.

| 키 | 스킬 | ST 캐스트 | M+ 캐스트 | 비고 |
| --- | --- | --- | --- | --- |
| `1` | 살상 | 51.6 (13.4 CPM) | 451.7 (15.1 CPM) | 양쪽 1위 고정 |
| `2` | 코브라 | 38.8 (10.1 CPM) | 220.3 (7.4 CPM) | ST 2위 |
| `3` | 날사 | 29.7 (7.7 CPM) | 331.9 (11.1 CPM) | M+ 2위 — 코브라와 **콘텐츠별 역전**이라 인접 배치 |
| `4` | 난타 | 0 (`actions.st`의 `wild_thrash`는 `active_enemies>1` 게이트) | 캐스트 수 미확인, 쿨 상한 7.5 CPM | 광역 전용 |
| `5` | 격노 | 300초당 10회 (30초 고정 쿨, 헤이스트 무관) | 동일 | 최저 빈도 |

출처: [Archon 12.1 BM Raid — Chimaerus Normal](https://www.archon.gg/wow/builds/beast-mastery/hunter/raid/rotation/normal/chimaerus) (24 파스, 전투 ~230초, `Casts` 열 직독) / [Archon BM M+ — Algeth'ar Academy +10](https://www.archon.gg/wow/builds/beast-mastery/hunter/mythic-plus/rotation/10/algethar-academy/this-week) (23 파스, 합계 ~1793초).
⚠️ **M+ 표본은 Archon 라벨이 "Midnight, Season 1"** — 12.1 M+ 캐스트 통계는 공개 출처에서 확인하지 못했다. 난타·격노의 로그 캐스트 수도 미확인이며 쿨 상한만 확정이다.

**이 순서를 DPS 최적화로 오해하지 말 것.** 입력은 합성 키라 자리 배치가 DPS를 바꾸지 않는다.

`05 격노`는 `[조건] 격노토글`로만 막는다. exec를 끄지 않는다.

대기: 임박 + 날사 0 + 포효 아님 + 송곳니&lt;3 → `13`/`14` 둘 다 불성립. 홀드(ready·토글 박스 꺼짐)면 `07`/`10`/`13`이 유지되고 `05`만 안 나감.

살상 2연타 latch는 없다. `[조건] 동맹`/`[조건] 포효`가 다음 루프에 꺼져야 한다.

### 9.3 센서가 빠질 때

| 없는 센서 | 하는 일 |
| --- | --- |
| `[조건] 다수2` | `02` `03` 비활성·`04` 삭제. ST 전용 |
| `[조건] 난타레디` | `04` 삭제 |
| `[조건] 포효임박4` | `10`에서 해당 비활성만 삭제 |
| `[조건] 격노토글` | `05` 삭제. 격노는 수동 키 `5`만 |
| `[조건] 송곳니3` | `11` 삭제 |

### 9.4 넣지 않는 것

| 항목 | 이유 |
| --- | --- |
| KeySim runtime toggle / F6 | 맥 전역 키 대신 애드온 박스 |
| `살상안전` / 중쿨 1충 (`09`) | 12.1에서 두 쿨 비교 불가. 1충은 `10`만 |
| `회전베기` `다수4` 송곳니 1스택 | 캡처 대비 DPS 작음 |
| `색_*` / AC 필러 | 홀드 중 next가 안 바뀜 |
| 장신구·물약·종족 | 격노에 수동 |
| 날사 타겟 스왑 | §7 |
| 어둠 순찰자 | 별도 |

---

## 10. 스킬명 대조

한국어는 와우 클라이언트(Wowhead KO) 표기. 줄임은 이 문서·프로필에서 쓰는 말.

| 영문 | 한국어 | 줄임 |
| --- | --- | --- |
| Barbed Shot | 날카로운 사격 | 날사 |
| Bestial Wrath | 야수의 격노 | 격노 |
| Kill Command | 살상 명령 | 살상 |
| Cobra Shot | 코브라 사격 | 코브라 |
| Wild Thrash | 마구잡이 난타 | 난타 |
| Beast Cleave | 야수의 회전베기 | 회전베기 |
| Howl of the Pack Leader | 무리의 지도자의 포효 | 포효 |
| Nature's Ally | 자연의 동맹 | 동맹 |
| Cobra Fang | 코브라 송곳니 | 송곳니 |
| Arcane Shot | 신비한 사격 | 신사 |
| Stampede! | 쇄도! | 쇄도 |
| Black Arrow | 검은 화살 | 검화 |
| Wailing Arrow | 죽음의 통곡 | 통곡 |
| Withering Fire | 부패의 사격 | 부패 |
| Pack Leader | 무리의 지도자 | — |
| Dark Ranger | 어둠 순찰자 | — |
| Hogstrider | 멧돼지 기수 | — |

---

> **개정 이력**
>
> - 2026-08-14 초판 — 로컬 스냅샷(fetch 2026-08-03) 기준.
> - 2026-08-14 개정 — `midnight` 브랜치 라이브 재확인. `actions.st` 1순위에 오버캡 조건 병합, `kill_command` 2줄 분리(`howl_summon.ready` 신설), `cobra_fang` 신설, `actions.cleave`에서 `hogstrider` 삭제·`bestial_wrath` 위치 이동·`prev.wild_thrash` 제거를 반영. §2·§3·§5·§6·§7 수정.
> - 2026-08-14 키심 표 추가. 적 수는 AC. 난타는 `색_난타`만.
> - 2026-08-18 §9 전체 이벤트. 살상 1충/2충 분리, 포효 4초·격노 3초 플래그, `색_살상` 입력 제외, 축소표.
> - 2026-08-18 §9 재작성. AC-next 폐기, 임박에 ready 포함, 3초 폐기, 광역 분리, 독립 필러.
> - 2026-08-18 조건 접두어 `[조건]`, 입력 `01`–`15` 이름 정렬.
> - 2026-08-18 교차 검증 반영 — 헤더 스냅샷 상태 정정, §3 게이트 폭, §9 키 자리표시 빈도순.
> - 2026-08-18 §9 DPS 핵심만. 센서 11·입력 9. 전부 `priority=1`, 이름 `01`–`09` iteration. `격노여유` 양성.
> - 2026-08-18 `07`에 `살상안전`, `08 코브라_송곳니` 추가. 입력 `01`–`10`.
> - 2026-08-18 격노 runtime toggle(`F6`, `05`만 멤버). 임박에서 ready 제외, 여유=임박 반대. `03 날사_패킹준비`·`09 살상_동맹1준비`. 입력 `01`–`12`.
> - 2026-08-18 `[조건] 포효임박4` + `08` 비활성. 1충 포효 4초 홀드.
> - 2026-08-18 `격노여유1.5` 삭제. 여유를 `격노준비` / `임박·준비 둘 다 꺼짐` 두 행으로 분할. 입력 `01`–`14`.
> - 2026-08-18 `09 살상_동맹1`·`[조건] 살상안전` 삭제(중쿨 1충 포기). `10`에 비활성 `포효임박4`. 입력 `01`–`08`·`10`–`14`.
> - 2026-08-18 KeySim runtime toggle 제거. `[조건] 격노토글` + `05` 활성. 홀드는 애드온 박스.
>
> `assisted_combat` APL은 두 시점 모두 동일하므로 AC 관련 분석(§1·§4)은 영향 없다.
> 로컬 `simc-apl-research` 스냅샷은 `f50a2121bf89`(fetch 2026-08-14)로 갱신 완료 — `SOURCE.txt` 확인함.
