# 전사 — KeySim 로테이션 규칙

**작성일:** 2026-08-14
**상위 문서:** [`midnight-rotation-handoff.md`](midnight-rotation-handoff.md)
**근거:** `simulationcraft/simc` `midnight` 라이브 APL (2026-08-14 fetch) + Icy Veins 12.1
**전제:** AC(JustAC / Shinkili)를 필러로 두고, 이산 신호만 예외로 얹는다. 장신구·물약·종족기·차단·큰 생존 쿨은 핸드오프 §4대로 **수동**.

12.1 추천 영웅 특성: 분노·무기 **학살자**, 방어 **산의 영주** (레이드·미딕).

---

## A. 분노

### A.1 기본 진단

AC는 `격노 꺼짐 → 광란`까지는 있다. **무모한 희생·화신도 AC 본문에 있다** — `recklessness,if=cooldown.slam.ready` / `avatar,if=cooldown.slam.ready` (assisted_combat 4·5행). `actions.cooldowns`에도 중복 등재돼 있을 뿐, "어시스트가 안 고르는 줄"이 아니다.
default `actions.slayer` 갭은 **격노 잔여 <1글쿨 패킹**, 무모한 희생 창 안 재배치, 분노 100 홀드다.

### A.2 확정 규칙 (학살자)

```
1. 격노 버프 꺼짐                         → 광란
2. 급살                                   → 마무리
3. 난도질                                 → 분쇄의 일격(창 안) / 분노의 강타
4. 소용돌이 / 광역                         → AC 추천 추종 (적 수는 AC)
5. 이하 AC 추천 추종
```

| 규칙 | 근거 | 비고 |
| --- | --- | --- |
| 1 | `slayer:48` `rampage,if=buff.enrage.remains<gcd` | 잔여 <1글쿨은 관측이 어려움. **꺼짐**만 확정.<br>⚠️ **AC에 이미 있다** (`rampage,if=buff.enrage.down`) → 예외 레이어에 둘 필요가 없다 |
| 2 | `slayer:61` `execute,if=buff.sudden_death.up`<br>(`slayer:52`는 무희 중 한정으로 더 위) | 2중첩까지 쌓인다는 서술은 fury APL에 근거가 없다(`stack` 조건은 **무기**에만 존재) — 미확인 |
| 3 | `thane:90` `crushing_blow,if=buff.hack_and_slash.up`<br>`thane:95` `raging_blow,if=buff.hack_and_slash.up` | ⚠️ **산의 영주(`actions.thane`) 줄이다.** 이 절 제목은 (학살자)인데 근거가 다른 영웅 트리다.<br>`slayer:54`는 조건 없는 `crushing_blow`, `slayer:53`은 `rampage,if=buff.recklessness.up&!buff.hack_and_slash.up` 로 **부정 조건**이다 |
| 4 | AC가 소용돌이를 고름 | 키심에 적 수 조건을 넣지 않는다 |

### A.3 남은 추가

| 규칙 | 근거 |
| --- | --- |
| 무모한 희생 켜짐 AND 칼날폭풍 준비 → 칼날폭풍 | `bladestorm, if=buff.recklessness.up` — 직전 광란은 이상일 뿐 강제 아님 |
| 산의 영주: 천둥 작렬 2중첩 → 천둥 작렬 | `thunder_blast, if=buff.thunder_blast.stack=2` |
| 무모한 희생·화신 준비 | 표시 또는 수동. AC 풀에 없을 수 있음 |

### A.4 구현 불가

분노 100 이상 광란, 격노 잔여 <1글쿨, 창 안 스킬 미세 재배치, 학살자/산의 영주 자동 전환.

### A.5 §4 편차

무모한 희생·화신은 1분급 큰 쿨이다. 키심 예외 레이어에 **넣지 않는다.**

다만 AC 본문 4·5행이 이 둘을 추천하므로, **F 슬롯을 그대로 추종하면 이미 자동 입력된다.** "AC가 안 고르면 넣지 않는다"가 아니라 **"필러에서 빼지 않으면 자동된다"** 가 사실이다. 핸드오프 §4를 지키려면 필러 매핑에서 제외해야 한다. (BM 문서 §8과 동일 사안)

---

## B. 무기

### B.1 기본 진단

AC는 짧은 평탄 리스트(분쇄 없으면 유지 → 휩쓸기 3+ → 파괴자 → 거강 → 필사 → 마무리 → 제압 → 격돌).
default는 거신/학살자 × 단타/광역/처형 **6리스트**. 색으로 전부 못 따라간다. 목표는 **AC + 창/캡/프록**.

### B.2 확정 규칙 (학살자)

```
1. 제압 2충전                              → 제압
2. 기회주의자 2중첩                        → 제압
3. 급살 2중첩                              → 마무리
4. 거강 디버프 on AND 칼날폭풍 준비        → 칼날폭풍
5. 휩쓸기                                  → AC 추천 추종 (적 수는 AC)
6. 이하 AC 추천 추종
```

| 규칙 | 근거 | 비고 |
| --- | --- | --- |
| 1 | `overpower, if=talent.dreadnaught&charges=2` (광역) | 캡 방지. 단타도 동일하게 둠 |
| 2 | `overpower, if=buff.opportunist.stack=2` | |
| 3 | `execute, if=buff.sudden_death.stack=2` | 1중첩은 AC에 맡김 |
| 4 | `bladestorm, if=debuff.colossus_smash.up\|buff.avatar.up` | 거강 **안**에 쓴다 |
| 5 | AC가 휩쓸기를 고름 | 키심에 적 수 조건을 넣지 않는다 |

거신이면 규칙 4를 **거강 디버프 AND 철거 준비 → 철거**로 바꾼다 (`demolish, if=...colossus_smash.up`).

### B.3 남은 추가

| 규칙 | 근거 |
| --- | --- |
| 거강 직전 파괴자 | `ravager, if=cooldown.colossus_smash.remains<=gcd` |
| 전쟁의 지배자 → 영웅의 일격 | AC에도 있음. 보험 |
| 부수 피해 3 → 회전베기 | 중첩 관측 가능할 때만 |

### B.4 구현 불가

분쇄 잔여 수식, 거인의 힘/집행자의 정밀 중첩 운용, 처형(20/35%) 리스트 전환, 날사식 멀티닷.

### B.5 §4 편차

거인의 강타는 짧은 로테이션 창이다. AC가 추천하면 따라가도 된다. 화신·파괴자는 거강과 맞출지 **수동**이 안전하다.

---

## C. 방어

핸드오프는 탱 본업을 제외했지만, **공격 필러 + 이산 프록**만 적는다. 방패의 벽·최후의 저항 등 큰 생존은 수동.

### C.1 기본 진단

AC: 전투의 외침 → **화신** → 파괴자 → 철거 → 천둥 작렬(프록) → 방패 밀쳐내기 → 천둥벼락 → 복수(4+) → **방패 돌진** → 마무리 → 복수 → 파괴의 일격.
default는 고통 감내 분노 수식, 방패 막기 `remains<=10`, 영웅 트리 분기가 많다. 수식은 포기한다.

### C.2 확정 규칙

```
1. 천둥 작렬 2중첩                         → 천둥 작렬
2. 복수 프록(무료)                         → 복수
3. 방진 없음 AND 천둥벼락 준비             → 천둥벼락 (거신 단타)
4. 이하 AC 추천 추종 (방패 밀쳐내기가 뼈대)
```

| 규칙 | 근거 | 비고 |
| --- | --- | --- |
| 1 | `31행` `thunder_blast,if=talent.booming_voice&buff.thunder_blast.stack=2&talent.snap_induction`<br>`35행` `thunder_blast,if=spell_targets.thunder_blast>=2&buff.thunder_blast.stack=2` | ⚠️ **조건 없는 2중첩 줄은 없다.** 둘 다 특성 또는 대상 수 조건이 함께 붙는다.<br>둘 다 **최상위 공통 블록**이다(산의 영주 전용 아님). `thane_st:75`·`thane_aoe:67`은 오히려 **무조건** `thunder_blast` |
| 2 | `colossus_st:57` · `colossus_aoe:45` · `thane_st:78` · `thane_aoe:70` `revenge,if=buff.revenge.up` | 네 리스트 모두에 존재. 12.1 세트 관련 서술은 APL에 근거가 없다 — 별도 출처 필요 |
| 3 | `colossus_st:55` `thunder_clap,if=!buff.phalanx.up` (같은 줄이 `65행`에도) | ⚠️ **`colossus_st` 리스트의 첫 줄이다** — 복수보다 위. 아래 키심 표의 P30(필러 밑)은 이 순서와 어긋난다.<br>`colossus_st` 전용이므로 산의 영주에는 적용하지 말 것 |

### C.3 남은 추가

| 규칙 | 근거 |
| --- | --- |
| 거인의 힘 3+ AND 철거 → 철거 | `demolish, if=buff.colossal_might.stack>=3` |
| 방패 막기 버프 ≤10초 → 방패 막기 | `shield_block, if=buff.shield_block.remains<=10` — 유지기. 큰 쿨 아님 |
| 힘의 폭발 → 방패 밀쳐내기 연속 | 프록이 아이콘이면 |

고통 감내는 분노 수식을 그대로 못 그린다. `[조건] 분노충분` 이산 박스(적절한 분노 이상)로만 덤프한다.

### C.4 구현 불가

분노 deficit 수식, 방패 막기 스윙 타이밍, 화신 중 천둥벼락 격글쿨 교차의 완전 재현.

### C.5 §4 편차

화신·파괴자·사기의 외침·방패 돌진은 §C.6 `[조건] CD토글`. 방패 막기는 로테이션 유지기여서 예외에 넣는다.

### C.6 키심 이벤트·조건 (DPS 핵심만)

전제: **산의 영주 (`talent.lightning_strikes`).** SimC 공통 접두 + `thane_st`. `thane_aoe`(3타+)·거신 리스트는 넣지 않는다. 조건은 AND만. 센서는 `execute_action=False`.

입력 **우선순위는 전부 `1`.** 같은 그룹은 `min((priority, name))`이므로 접두 `01`–`11`이 순서다. 입력은 화면 없는 조건 AND이므로 `use_event=true`.

**GCD를 그룹으로 나눈다.** `_select_by_group_priority`는 그룹당 1개 + 무그룹은 전부. GCD 스킬과 off-GCD 스킬을 한 그룹에 두면 off-GCD가 작렬을 영구 선점한다.

| 그룹 | GCD | 멤버 | 한 루프 |
| --- | --- | --- | --- |
| `방어오프` | 아님 | 화신, 방패 막기, 고통 감내 | 이 중 1개 (`01`>`05`>`06`) |
| `방어` | 포함 | 파괴자, 사외, 돌진, 작렬, 밀쳐내기, 마무리, 복수, 벼락 | 이 중 1개 (`02`>`03`>`04`>`07`…`11`) |

같은 루프에서 그룹마다 하나씩 나간다. 화신(오프)+파괴자(GCD), 방막/감내+작렬이 같이 나갈 수 있다.

근거: Wowhead 12.1 주문 GCD — 화신·방패 막기·고통 감내 `0s`, 파괴자 `1.5s`. SimC `ignore_pain`은 `use_off_gcd=1`. 방패 막기·고통 감내는 1초 내부 쿨.

라이브 `default/warrior_protection.simc` (`f50a2121bf89`): `thane_st`/`thane_aoe` 첫 줄은 **무조건 천둥 작렬**. 접두 `avatar`는 `buff.thunder_blast.down|stack<=2`(최대 2면 사실상 항상). `ravager`는 무조건.

KeySim runtime toggle은 쓰지 않는다. 신키리(또는 애드온)가 허용 박스를 그리고, 키심은 `[조건] CD토글`로만 본다. 박스 꺼짐 = 홀드(`01`–`04` 불성립), 켜짐 = 화신·파괴자·사외·돌진 자동. 필러는 토글과 무관하다. 키 `8`/`9`/`0`/`F` 수동은 조건과 무관하다. 프로필 `runtime_toggle_enabled`는 끈다.

고통 감내: `[조건] 분노충분`이 켜진 동안만, **GCD 밖에서** 소모. 수식은 그리지 않는다. 애드온이 “적절한 분노 이상”만 켠다. `방어오프` 안에서는 방막이 감내보다 앞선다.

#### C.6.1 감지

| 이벤트 | 점등 | 겹침 |
| --- | --- | --- |
| `[조건] 화신준비` | 화신 ready | |
| `[조건] 파괴자준비` | 파괴자 ready | |
| `[조건] 사외준비` | 사기의 외침 ready | |
| `[조건] 돌진준비` | 방패 돌진 ready | |
| `[조건] CD토글` | 신키리 화신·파괴자·사외·돌진 허용 박스 | 꺼짐 = 홀드 |
| `[조건] 방막임박` | 방패 막기 버프 `remains≤10` 또는 없음 | |
| `[조건] 분노충분` | 고통 감내 덤프 임계 이상 | 꺼짐 = 감내 안 함 |
| `[조건] 작렬레디` | 천둥 작렬 ready | |
| `[조건] 방패 밀쳐내기` | 방패 밀쳐내기 준비 | |
| `[조건] 마무리가능` | 마무리 사용 가능(급살 또는 처형 체력) | |
| `[조건] 복수프록` | 복수 무료 프록 | |
| `[조건] 벼락레디` | 천둥벼락 준비 | |

#### C.6.2 입력 (전부 `priority=1`)

| 이벤트 | 그룹 | GCD | 키 | 활성 |
| --- | --- | --- | --- | --- |
| `01 화신` | `방어오프` | 아님 | 화신 | `[조건] 화신준비` `[조건] CD토글` |
| `02 파괴자` | `방어` | 포함 | 파괴자 | `[조건] 파괴자준비` `[조건] CD토글` |
| `03 사외` | `방어` | 포함 | 사기의 외침 | `[조건] 사외준비` `[조건] CD토글` |
| `04 돌진` | `방어` | 포함 | 방패 돌진 | `[조건] 돌진준비` `[조건] CD토글` |
| `05 방막_유지` | `방어오프` | 아님 | 방패 막기 | `[조건] 방막임박` |
| `06 고통감내` | `방어오프` | 아님 | 고통 감내 | `[조건] 분노충분` |
| `07 작렬` | `방어` | 포함 | 천둥 작렬 | `[조건] 작렬레디` |
| `08 방패 밀쳐내기` | `방어` | 포함 | 밀쳐내기 | `[조건] 방패 밀쳐내기` |
| `09 마무리` | `방어` | 포함 | 마무리 | `[조건] 마무리가능` |
| `10 복수_프록` | `방어` | 포함 | 복수 | `[조건] 복수프록` |
| `11 벼락` | `방어` | 포함 | 천둥벼락 | `[조건] 벼락레디` |

`[조건] CD토글` 꺼짐이면 `01`–`04`만 멈춘다. 방막·감내·작렬·밀쳐내기·마무리·복수·벼락은 유지. `06`은 `07`을 막지 않는다.

**키 자리표시.** 공격 필러 빈도 감. 생존 쿨이 아님.

| 키 | 스킬 |
| --- | --- |
| `1` | 방패 밀쳐내기 |
| `2` | 복수 |
| `3` | 천둥벼락 |
| `4` | 마무리 일격 |
| `5` | 천둥 작렬 |
| `6` | 고통 감내 |
| `7` | 방패 막기 |
| `8` | 화신 |
| `9` | 파괴자 |
| `0` | 사기의 외침 |
| `F` | 방패 돌진 |

#### C.6.3 센서가 빠질 때

| 없는 센서 | 하는 일 |
| --- | --- |
| `[조건] 화신준비` | `01` 삭제. 화신은 수동 |
| `[조건] 파괴자준비` | `02` 삭제. 파괴자는 수동 |
| `[조건] 사외준비` | `03` 삭제. 사외는 수동 |
| `[조건] 돌진준비` | `04` 삭제. 돌진은 수동 |
| `[조건] CD토글` | `01`–`04` 삭제. 넷은 수동 키 `8`/`9`/`0`/`F`만 |
| `[조건] 방막임박` | `05` 삭제 |
| `[조건] 분노충분` | `06` 삭제. 감내는 수동 |
| `[조건] 마무리가능` | `09` 삭제 |
| `[조건] 복수프록` | `10` 삭제 |

#### C.6.4 넣지 않는 것

| 항목 | 이유 |
| --- | --- |
| KeySim runtime toggle / F6 | 야수와 동일. 맥 전역 키 대신 화면 박스 |
| `색_*` / AC 필러 | 야수와 동일. 홀드·게이트가 next를 안 바꿈 |
| 거신 철거 / 거인의 힘 / 방진 벼락 | 12.1 메타는 산의 영주 |
| `thane_aoe` 방진 밀쳐내기·3타 벼락 선행 | 대상 수 분기. ST 리스트로 통일 |
| 화신 전 작렬 잔여 소진 | Icy Veins 권고. SimC 접두는 사실상 무조건 화신 |
| 분노 deficit 수식 전부 | 이산 `[조건] 분노충분`만 |
| `snap_induction` 작렬 2줄 | 특성 게이트 |
| 무조건 복수 잔여 | 분노 숫자 |

---

## 키심 이벤트·조건

`감_` = 감지(실행 끔). `색_` = Shinkili 추천 색. `priority`는 작을수록 먼저. **적 수는 AC.** 무희·화신·방패의 벽은 입력 안 함.

### 분노 (그룹 `분노`)

| 이벤트 | 찍는 것 |
| --- | --- |
| `감_격노꺼짐` | 격노 버프 없음 |
| `감_급살` | 급살 |
| `감_난도질` | 난도질 |

| P | 이벤트 | 키 | 활성 필요 |
| --- | --- | --- | --- |
| 10 | `광란_격노` | 광란 | `감_격노꺼짐` |
| 20 | `마무리_급살` | 마무리 | `감_급살` |
| 30 | `분쇄_난도질` | 분쇄의 일격 / 분노의 강타 | `감_난도질` |
| 90 | `색_*` | AC가 고른 키 | 해당 `색_*` |

### 무기 (그룹 `무기`, 학살자)

| 이벤트 | 찍는 것 |
| --- | --- |
| `감_제압2` | 제압 2충전 |
| `감_기회2` | 기회주의자 2중첩 |
| `감_급살2` | 급살 2중첩 |
| `감_거강` | 거인의 강타 디버프 |
| `감_칼폭준비` | 칼날폭풍 준비 |

| P | 이벤트 | 키 | 활성 필요 |
| --- | --- | --- | --- |
| 10 | `제압_캡` | 제압 | `감_제압2` |
| 20 | `제압_기회` | 제압 | `감_기회2` |
| 30 | `마무리_급살2` | 마무리 | `감_급살2` |
| 40 | `칼폭_거강` | 칼날폭풍 | `감_거강` `감_칼폭준비` |
| 90 | `색_*` | AC가 고른 키 | 해당 `색_*` |

거신이면 P40을 `감_거강` + 철거 준비 → 철거.

### 방어

§C.6. 프로필 `전사_방어`. 산의 영주. `색_*` 없음. 화신·파괴자·사외·돌진은 `[조건] CD토글`.

---

## 스킬명 대조

| 영문 | 한국어 | 줄임 |
| --- | --- | --- |
| Recklessness | 무모한 희생 | 무희 |
| Avatar | 화신 | — |
| Rampage | 광란 | — |
| Enrage | 격노 | — |
| Bloodthirst | 피의 갈증 | 피갈 |
| Raging Blow | 분노의 강타 | — |
| Crushing Blow | 분쇄의 일격 | — |
| Bloodbath | 피바다 | — |
| Odyn's Fury | 오딘의 분노 | 오딘 |
| Bladestorm | 칼날폭풍 | — |
| Execute | 마무리 일격 | 마무리 |
| Sudden Death | 급살 | — |
| Hack and Slash | 난도질 | — |
| Whirlwind | 소용돌이 | 분풍 |
| Improved Whirlwind | 강화 소용돌이 | — |
| Thunder Blast | 천둥 작렬 | — |
| Thunder Clap | 천둥벼락 | — |
| Mortal Strike | 필사의 일격 | 필사 |
| Overpower | 제압 | — |
| Colossus Smash | 거인의 강타 | 거강 |
| Rend | 분쇄 | — |
| Cleave | 회전베기 | — |
| Sweeping Strikes | 휩쓸기 일격 | 휩쓸기 |
| Slam | 격돌 | — |
| Heroic Strike | 영웅의 일격 | — |
| Ravager | 파괴자 | — |
| Demolish | 철거 | — |
| Opportunist | 기회주의자 | — |
| Shield Slam | 방패 밀쳐내기 | 밀쳐내기 |
| Revenge | 복수 | — |
| Shield Block | 방패 막기 | — |
| Ignore Pain | 고통 감내 | — |
| Shield Wall | 방패의 벽 | — |
| Shield Charge | 방패 돌진 | — |
| Demoralizing Shout | 사기의 외침 | 사외 |
| Phalanx | 방진 | — |
| Colossal Might | 거인의 힘 | — |
| Slayer | 학살자 | — |
| Colossus | 거신 | — |
| Mountain Thane | 산의 영주 | — |

---

> **개정 이력**
>
> - 2026-08-14 초판 — midnight 라이브 APL + Icy Veins 12.1. 이산 플래그만 확정.
> - 2026-08-14 키심 표 추가. 적 수는 AC.
> - 2026-08-14 검증 — 로컬 재fetch본(`f50a2121bf89`)과 한 줄씩 대조. 근거 인용에 리스트·행 번호 표기. 확정된 것만 반영:
>   §A.1·§A.5 편차 서술 반전(AC 본문이 무희·화신을 추천), §A.2 규칙 3 근거가 `thane` 줄임(절 제목은 학살자),
>   §C.1 AC 순서에 화신·방패 돌진 누락, §C.2 규칙 1 조건 누락·리스트 귀속 오류, 규칙 3 순서 어긋남.
>
> **§B(무기)는 수정 없음** — 규칙 1~4와 §B.3 파괴자 인용이 모두 `actions.slayer_st` / `slayer_aoe` 원문과 일치함을 확인.
>
> - 2026-08-18 §C.6 거신 단타 스켈레톤. `colossus_st` 첫 줄 밀쳐내기. 입력 `01`–`08`, `색_*`·키심 토글 없음.
> - 2026-08-18 §C.6 산의 영주. `thane_st` 작렬 무조건. 고통 감내=`[조건] 분노충분`. 화신·파괴자만 `F6` 토글 멤버. 입력 `01`–`09`.
> - 2026-08-18 GCD/`방어`와 off-GCD/`방어오프` 분리. 화신·방막·감내는 작렬과 같은 루프.
> - 2026-08-18 화신·파괴자 KeySim toggle 제거. `[조건] CD토글` + `01`/`02` 활성.
> - 2026-08-18 사외·돌진 추가. 넷 모두 `[조건] CD토글`. 입력 `01`–`11`.
>
> **미확정(손대지 않음):** 분노 급살 "2중첩" 서술, 방어 복수의 12.1 세트 효과 — 둘 다 APL로는 확인·반증 모두 불가.
